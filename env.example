# Database Configuration
POSTGRES_USER=warehouse_user
POSTGRES_PASSWORD=warehouse_password
POSTGRES_DB=warehouse_db

# Backend Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CORS_ORIGINS=http://localhost:3000,http://localhost:80
NODE_ENV=production
PORT=4000

# Email / Notification Configuration
APP_NAME=Warehouse
APP_BASE_URL=http://localhost:5173
SUPPORT_EMAIL=support@warehouse.local
EMAIL_FROM="Warehouse" <no-reply@warehouse.local>
ADMIN_EMAILS=admin@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-google-app-password
SMTP_TIMEOUT_MS=8000
PASSWORD_RESET_EXPIRY_MINUTES=30

# Frontend Configuration
VITE_API_URL=http://localhost:4000

# Database URL (automatically constructed from above, or set directly)
# DATABASE_URL=postgresql://warehouse_user:warehouse_password@postgres:5432/warehouse_db

